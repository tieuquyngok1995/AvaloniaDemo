<UserControl
  x:Class="ConfigGenerator.Views.SensorDataCollectorSettingsView"
  xmlns="https://github.com/avaloniaui"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:vm="clr-namespace:ConfigGenerator.ViewModels"
  d:DesignHeight="600"
  d:DesignWidth="800"
  x:DataType="vm:SensorDataCollectorSettingsViewModel"
  mc:Ignorable="d">

  <Grid RowDefinitions="Auto, *">
    <!--  Header  -->
    <Border Grid.Row="0" Padding="10">
      <TextBlock Classes="header" Text="情報取得アプリの設定" />
    </Border>

    <!--  Content  -->
    <Border Grid.Row="1" Padding="10">
      <Grid ColumnDefinitions="*, *" RowDefinitions="Auto, Auto, Auto, Auto">
        <StackPanel
          Grid.Row="0"
          Orientation="Vertical"
          Spacing="4">
          <TextBlock Text="センサー情報取得元" />
          <CheckBox Content="RoomSense中継機" IsChecked="{Binding UiState.UseJsonServer, Mode=TwoWay}" />
          <CheckBox Content="RoomSenseクラウド" IsChecked="{Binding UiState.UseCloud, Mode=TwoWay}" />
        </StackPanel>

        <!--  DataInputMode: 0  -->
        <StackPanel
          Grid.Row="1"
          Grid.Column="0"
          Margin="4"
          IsVisible="{Binding UiState.UseJsonServer}"
          Orientation="Vertical"
          Spacing="4">
          <TextBlock Text="RoomSense中継機 - IP:Port" />
          <TextBox
            Classes="multiline"
            Text="{Binding UiState.JsonServerEndpoints, Mode=TwoWay}"
            Watermark="IPアドレスとポート番号" />
        </StackPanel>
        <!--  DataInputMode: 1  -->
        <StackPanel
          Grid.Row="1"
          Grid.Column="{Binding UiState.CloudColumn}"
          Margin="4"
          IsVisible="{Binding UiState.UseCloud}"
          Orientation="Vertical"
          Spacing="4">
          <TextBlock Text="RoomSenseクラウド - 中継機コード:APIキー" />
          <TextBox
            Classes="multiline"
            Text="{Binding UiState.RoomSenseEndpoints, Mode=TwoWay}"
            Watermark="中継機コードとAPIキーを入力" />
        </StackPanel>

        <Border
          Grid.Row="2"
          Grid.ColumnSpan="3"
          Margin="0,8">
          <Grid ColumnDefinitions="*, *, *" RowDefinitions="Auto, Auto, *">
            <!--  ExecutionIntervalInSeconds  -->
            <StackPanel
              Grid.Row="0"
              Orientation="Vertical"
              Spacing="4">
              <TextBlock Text="センサー情報取得間隔（秒）" />
              <TextBox Text="{Binding DataModel.ExecutionIntervalInSeconds, Mode=TwoWay}" Watermark="秒数を入力（例: 60）" />
            </StackPanel>

            <StackPanel
              Grid.Row="1"
              Orientation="Vertical"
              Spacing="4">
              <TextBlock Text="出力モード選択" />
              <ComboBox
                HorizontalAlignment="Stretch"
                ItemsSource="{Binding UiState.DataOutputModes}"
                MaxDropDownHeight="100"
                SelectedIndex="0"
                SelectedItem="{Binding UiState.SelectedDataOutputMode}">
                <ComboBox.ItemTemplate>
                  <DataTemplate>
                    <TextBlock Text="{Binding DisplayText}" />
                  </DataTemplate>
                </ComboBox.ItemTemplate>
              </ComboBox>
            </StackPanel>

            <!--  DataOutputMode: 0  -->
            <StackPanel
              Grid.Row="1"
              Grid.Column="1"
              IsVisible="{Binding UiState.UseJsonFileVisible}"
              Orientation="Vertical"
              Spacing="4">
              <TextBlock Text="JSONファイル保存先" />
              <TextBox Text="{Binding DataModel.JsonFilePath, Mode=TwoWay}" Watermark="保存するファイル名を入力" />
            </StackPanel>
            <!--  DataOutputMode: 1  -->
            <StackPanel
              Grid.Row="1"
              Grid.Column="1"
              IsVisible="{Binding UiState.UseJsonServerVisible}"
              Orientation="Vertical"
              Spacing="4">
              <TextBlock Text="JSON Server サーバーURL" />
              <TextBox Text="{Binding DataModel.JsonServerUrl, Mode=TwoWay}" Watermark="JSON Server の URL を入力" />
            </StackPanel>
          </Grid>
        </Border>

        <StackPanel
          Grid.Row="3"
          Grid.ColumnSpan="2"
          HorizontalAlignment="Center">
          <Button Classes="primary">RoomSenseの情報を取得する</Button>
        </StackPanel>
      </Grid>
    </Border>
  </Grid>
</UserControl>
